local create_module = {}

create_module.cmd = {
  "CreateModule", function()
    -- nui input name of module
    -- create new module with init.lua file based on the input name.
    -- if exists assert err
  end
}

return create_module
